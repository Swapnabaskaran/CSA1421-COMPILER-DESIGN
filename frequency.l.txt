
%{
#include <stdio.h>
#include <string.h>
char target[50];
int count = 0;
%}

%%
[A-Za-z]+   {
                if(strcmp(yytext, target) == 0)
                    count++;
            }

[ \t\n]+    ;
.           ;

%%

int main()
{
    printf("Enter word to search: ");
    scanf("%s", target);
    printf("Enter sentence (Ctrl+D to stop):\n");
    yylex();
    printf("\nFrequency of '%s' = %d\n", target, count);
    return 0;
}

int yywrap(){ return 1; }
